services:
  - type: web
    name: awazsetu
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: MAPBOX_ACCESS_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.10.12

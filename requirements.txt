streamlit>=1.32
sentence-transformers>=2.2.2
streamlit-folium>=0.18
streamlit-js-eval>=0.1.5
folium>=0.15
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
vaderSentiment>=3.3.2
langdetect>=1.0.9
deep-translator>=1.11.4
Pillow>=9.0.0

# Optional — only needed if you want Gemini-powered translation instead of
# the built-in free local fallback. Not required for the app to run.
# google-generativeai>=0.5.0
